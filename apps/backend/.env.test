# Test Environment Configuration
# Used by run_prospect.py when running in test mode (default)

# Enable test mode - this replaces external API tools with mocks
ONEFORALL_TEST_MODE=true

# Database Configuration
# NOTE: Tests use the same Supabase instance but with TEST- prefixed data
# Consider using a separate test database in production
SUPABASE_URL=your_supabase_url_here
SUPABASE_KEY=your_supabase_key_here

# LLM Configuration
# DeepSeek for crew agents - REAL API calls for agent reasoning
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# Phoenix Observability (Optional)
# Set to 'true' to automatically start Phoenix UI
PHOENIX_AUTO_START=false

# Backend API URL (not used in test mode)
BACKEND_URL=http://localhost:8000

# External API Keys (NOT USED in test mode - mocks replace these tools)
# These are kept here for reference but won't be called
SENDGRID_API_KEY=not_needed_in_test_mode
TWILIO_SID=not_needed_in_test_mode
TWILIO_AUTH_TOKEN=not_needed_in_test_mode
TWILIO_NUMBER=not_needed_in_test_mode

# Test Mode Notes:
# - No real SMS/Email/WhatsApp messages will be sent
# - No real university/NSFAS submissions will be made
# - Student numbers will be prefixed with "TEST-"
# - Database writes still occur (use --cleanup flag to remove test data)
# - RAG/LLM queries still use real APIs for agent reasoning
